epochs: 200
batch_size: 16
lr: 1.0e-3
min_lr: 1.0e-5
weight_decay: 1.0e-5
grad_clip: 1.0

early_stop_patience: 30
physics_loss_weight: 0.0   # >0 enables the equilibrium-residual term (FNO only)
num_workers: 0
